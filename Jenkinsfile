@Library('my-shared-lib') _

pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                helloWorld()
            }
        }

        stage('Hi') {
            steps {
                hiWorld()
            }
        }
    }
}
